[tool.poetry]
package-mode = false
license = "MIT"

[tool.poetry.dependencies]
python = "^3.12"
gym = "0.22.0"
pygame = "^2.5.2"
setuptools = "^69.5.1"
scikit-learn = "^1.4.2"
torchvision = "^0.18.0"
torchaudio = "^2.3.0"
torch = "^2.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
